# Screenshots

Referenced from the main [README](../../README.md)'s "Screenshots" section. Not committed yet — add these
three PNGs here (any reasonable browser-window crop is fine, no fixed resolution required):

- `dashboard-overview.png` — the Streamlit dashboard: sidebar champion metadata plus a scored loan
  application (PD, risk band, decision, top reasons).
- `api-swagger.png` — the FastAPI Swagger UI at `http://localhost:8000/docs`.
- `mlflow-ui.png` — the MLflow run view at `http://localhost:5000` (`make mlflow-ui`), showing the champion
  run's params/metrics.

Once added, delete this placeholder note (or leave it — it won't break the README's image links either way).
